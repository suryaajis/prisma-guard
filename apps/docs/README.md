# PrismaGuard Docs

This is a placeholder app for the PrismaGuard documentation site.

The site itself (Next.js / Astro / Docusaurus) will be added in a subsequent
phase. The intent of this directory existing today is to lock in the monorepo
shape so that adding the docs site becomes a contained, reviewable change.
